# Runtime dependencies for red-media-browser.
#
# These are the direct third-party packages imported by src/. Transitive
# dependencies (e.g. websocket-client, update-checker) are pulled in and
# version-constrained automatically by praw and are intentionally not pinned
# here.
#
# Target the PRAW 8.x line (released 2026-06-14). The full call-site audit in
# TODO.md found the app is upgrade-clean against the PRAW 8 migration guide.
# Pinning the major below 9 prevents a future PRAW 9 from installing silently.

praw>=8,<9
prawcore>=4,<5
PyQt6>=6
python-vlc>=3
requests>=2
